// widgets.clear();
//
//
// for (var lindi in state) {
// widgets.add(
// SizedBox(
// key: lindi.key,
// width: 100,
// height: 100,
// child: Image.network(lindi.url),
// ),
// );
// }
// print('widgets length:${widgets.length}');
// lindiController.widgets.clear();
// lindiController.addAll(widgets);
// print('lindiController length:${lindiController.widgets.length}');
// print('all pass');